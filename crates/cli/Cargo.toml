[package]
name = "cli"
version = "0.1.0"
edition = "2024"

[dependencies]
moka_core = { path = "../core", package = "core" }
parsers = { path = "../parsers" }
anyhow = "1"
clap = { version = "4", features = ["derive"] }
